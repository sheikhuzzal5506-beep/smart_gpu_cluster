services:
  backend:
    build: ./backend
    container_name: gpu_scheduler_backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: gpu_scheduler_frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
    restart: unless-stopped